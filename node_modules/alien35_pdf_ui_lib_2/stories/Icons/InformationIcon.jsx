import { h } from 'preact';
import ChevronDown from '../../assets/information-circle.svg'; // Replace with the actual path
import ChevronDownWhite from '../../assets/information-circle-white.svg'; // Replace with the actual path
import { Icon } from '../Icon/Icon';
import { ThemeContext } from '../../Theme';

export const InformationIcon = ({ size = 'md' }) => (
  <ThemeContext.Consumer>
      {({ theme }) => (
        <Icon size={size} src={theme.name === "dark" ? ChevronDownWhite : ChevronDown} alt="Arrow left" />
      )}
    </ThemeContext.Consumer>
);