import { h } from 'preact';
import { ThemeContext } from '../../Theme';

export const Text = ({ children, style }) => {
  return (
    <ThemeContext.Consumer>
      {({ theme }) => (
        <span style={{ color: theme?.text, ...style }}>{children}</span>
      )}
    </ThemeContext.Consumer>
  );
};
