import { createContext } from 'preact';
import { colors } from './Colors';

export const ThemeContext = createContext({
  theme: 'light',
  toggleTheme: () => {},
});

export const themes = {
  light: {
    ...colors.light,
    name: "light",
    // other light theme styling...
  },
  dark: {
    ...colors.dark,
    name: "dark",
    // other dark theme styling...
  },
};